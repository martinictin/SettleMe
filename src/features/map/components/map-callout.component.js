import React from "react";

import { CompactRestaurantInfo } from "../../../components/products/compact-product-info.component";

export const MapCallout = ({ product }) => (
  <CompactRestaurantInfo isMap product={product} />
);
